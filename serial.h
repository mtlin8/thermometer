#include "project.h"

volatile extern int remoteTempOnes;
volatile extern int remoteTempTens;
volatile extern unsigned char receiving;
volatile extern unsigned char received;
volatile extern char count;
volatile extern char buf[6];